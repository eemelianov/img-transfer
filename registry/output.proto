
syntax = "proto3";

# for compatibility with waypoint builtin docker plugin we set the package to docker
package docker;

option go_package = "github.com/eemelianov/transporter/registry";

// Image is the artifact type for the registry.
message Image {
  string image = 1;
  string tag = 2;
}
